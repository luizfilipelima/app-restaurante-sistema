# E2E / Checkly — Copie para .env.e2e e preencha
# Para rodar: npm run test:e2e (usa variáveis do ambiente)
#
# IMPORTANTE: O .env principal (com VITE_SUPABASE_URL, etc.) deve existir
# para o app renderizar em dev. O Playwright inicia o Vite com npm run dev.

# Base URL — local dev ou produção
PLAYWRIGHT_BASE_URL=http://localhost:5173

# Admin (restaurante) — para order-flow e i18n
E2E_ADMIN_EMAIL=seu-admin@exemplo.com
E2E_ADMIN_PASSWORD=sua-senha

# Super-Admin — para super-admin-metrics
E2E_SUPER_ADMIN_EMAIL=super-admin@quiero.food
E2E_SUPER_ADMIN_PASSWORD=sua-senha

# Slug do restaurante de teste (para order-flow, i18n, smoke cardápio)
# Use um restaurante dedicado para Checkly para não sujar o banco
E2E_RESTAURANT_SLUG=meu-restaurante-teste
